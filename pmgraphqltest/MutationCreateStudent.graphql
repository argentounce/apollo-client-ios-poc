mutation MutationCreateStudent($input: CreateStudentInput!) {
  createStudent(input: $input) {
    id
    nickname
    year
    activeSection
    hadActiveSections
    hasRegisteredParent
    parents
    favoriteGames {
      id
      name
      playersPerMonth
      rating
      url
    }
  }
}
